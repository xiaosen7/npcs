{
  "$schema": "https://turbo.build/schema.json",

  "tasks": {
    "lint": {
      "inputs": [
        "src/**/*",
        ".storybook/**",
        "libs/**/*",
        "app/**/*",
        "tests/**/*"
      ]
    },
    "check-types": {
      "inputs": [
        "src/**/*",
        ".storybook/**",
        "libs/**/*",
        "app/**/*",
        "tests/**/*"
      ]
    },
    "test": {
      "inputs": ["src/**/*", "libs/**/*", "app/**/*", "tests/**/*"],
      "dependsOn": ["lint", "check-types"]
    },
    "build": {
      "inputs": [
        "src/**/*",
        "libs/**/*",
        "app/**/*",
        "next.config.mjs",
        "tailwind.config.ts",
        "prisma/**/*"
      ],
      "outputs": [".next/**", "!.next/cache/**"],
      "dependsOn": ["^compile"]
    },
    "prisma-generate-type-only": {
      "inputs": ["prisma/**/*"],
      "outputs": ["./node_modules/.prisma/**/*"],
      "cache": false
    },
    "compile": {
      "inputs": ["src/**/*"],
      "outputs": ["esm/**"],
      "dependsOn": ["^compile"]
    },
    "test:e2e": {
      "inputs": [
        "app/**",
        "libs/**",
        "src/**",
        "tests-e2e/**",
        "tsconfig.json",
        "package.json",
        "tailwind.config.*"
      ],
      "outputs": ["playwright-report/**"]
    },
    "test:changed": {
      "inputs": ["src/**/*", "libs/**/*", "app/**/*", "tests/**/*"],
      "dependsOn": ["lint", "check-types"]
    },
    "dev": {
      "persistent": true,
      "cache": false
    },
    "compile:watch": {
      "persistent": true,
      "cache": false
    },
    "test:watch": {
      "persistent": true,
      "cache": false
    }
  },
  "remoteCache": {
    "enabled": true
  }
}
