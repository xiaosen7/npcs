{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "esm/**"],
      "inputs": ["app/**", "libs/**", "src/**", "tsconfig.json", "package.json"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "test": {
      "dependsOn": ["^test"],
      "inputs": [
        "app/**",
        "libs/**",
        "src/**",
        "tsconfig.json",
        "package.json",
        "tests/**"
      ]
    },
    "dev": {
      "persistent": true,
      "cache": false
    }
  },
  "remoteCache": {
    "enabled": true
  }
}
