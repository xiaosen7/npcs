# app-template-ci.yml
name: Docker Build Notes

on:
  push:
    paths:
      - "apps/notes/**"
      - "libs/**"
      - "configs/**"
      - "package.json"
      - "pnpm-lock.yaml"
      - "turbo.json"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Call Docker Build Action
        uses: ./.github/actions/docker-build
        with:
          APP_DIR: apps/notes
          APP_PACKAGE_NAME: "@npcs/notes"
          IMAGE_NAME: ghcr.io/${{ github.actor }}/npcs-notes:latest
          PAT: ${{ secrets.PAT }}
          REGISTRY: ghcr.io
          TURBO_TEAM: ${{ secrets.TURBO_TEAM }}
          TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
          DOCKERFILE: ${{ secrets.DOCKERFILE }}
